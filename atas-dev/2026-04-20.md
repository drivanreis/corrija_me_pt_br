# Ata de desenvolvimento - 2026-04-20

### Decisão principal do dia
- A `LLM-Jandaia-1` saiu da esteira de produção.
- O backend passa a operar em modo `motor_only` como política oficial de runtime.
- A `Jandaia-1` fica mantida apenas para bancada/benchmark, sem participação no fluxo produtivo.

### Mudanças técnicas aplicadas
- `src/backend/server.ts`
  - removida chamada de fallback para LLM no fluxo `/v2/check-smart` e `/v2/check-core`;
  - payload `core` agora sempre aponta `targetLayer: "motor"` e `routeReason: "motor_only_runtime"`;
  - contrato `/health` e `/v2/architecture` ajustados para `llmCore.enabled=false` e runtime `motor_only`;
  - arquitetura interna atualizada para `fase_4_motor_only`.
- `scripts/smoke-test-node-app.mjs`
  - expectativas atualizadas para o novo runtime sem fallback LLM.
- `README.md`
  - documentação atualizada para deixar explícito que Jandaia é apenas laboratório.

### Validação executada hoje
- `npm run test:coverage`:
  - `100%` statements/branches/functions/lines.
- `npm run test:integration`:
  - smoke test + regressão externa concluídos com `0` falhas.
- `npm run test:continuous`:
  - execução concluída com geração e merge válidos.
- `npm test`:
  - suíte unitária + integração concluída com sucesso.

### Resultado operacional
- O produto permanece estável com o motor como única camada de correção em produção.
- A remoção da trilha LLM simplifica o runtime, reduz variabilidade de latência e elimina dependência de serviço externo/local no fluxo principal.
