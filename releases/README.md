# Releases

Esta pasta guarda os pacotes finais para distribuicao.

Arquivos esperados:

- `corrija_me_pt_br_windows_x64.zip`
- `corrija_me_pt_br_linux_x64.zip`

Para gerar:

```bash
npm run package:portable
```

Arquivos temporarios de compilacao continuam em `build/`.
